---
import { cx } from '../utils/cva.config'
import Link from './Link.astro'
const { className, href, ...rest } = Astro.props
---

<Link class={cx('inline-block border-b border-current', className)} href={href} {...rest}>
  <slot />
</Link>
